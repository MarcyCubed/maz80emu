[package]
name = "maz80emu"
version = "0.1.0"
edition = "2024"

[dependencies]
strum = { version = "0.28", features = ["derive"] }

[[bin]]
name = "runcpm"

[features]
# Show stuf while running a program
debugging = []

[profile.release]
debug = "line-tables-only"
